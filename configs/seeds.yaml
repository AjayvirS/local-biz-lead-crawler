directories:
  - name: "example_directory"
    start_urls:
      - "https://example.com/directory/page1"
    pagination:
      type: "next_link"
      selector: "a[rel='next']"
    rules:
      # optional: only keep outgoing links whose anchor text or surrounding element contains these words
      include_text_hints: ["Website", "Web", "Homepage"]
